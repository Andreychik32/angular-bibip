/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ToDayPipe } from './toDay.pipe';

describe('Pipe: ToDaye', () => {
  it('create an instance', () => {
    let pipe = new ToDayPipe();
    expect(pipe).toBeTruthy();
  });
});
